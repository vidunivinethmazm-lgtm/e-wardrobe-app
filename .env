# Loaded automatically by server/app.py (via python-dotenv) before it starts.
# Never commit this file — it's already in .gitignore.

# ── Stage: back-view generation + Gemini virtual try-on ────────────────────
# https://aistudio.google.com/apikey
GEMINI_API_KEY=AQ.Ab8RN6IBSjvADocJB2Gt3Jtr1nqFaXpCI7NzWiZV8HdZveb5NA
# Must be 0 for Gemini calls to actually happen (both the back-view
# auto-generation and, if selected, VIRTUAL_TRYON_PROVIDER=gemini).
AI_TRYON_MOCK=0

# ── Stage 1: virtual try-on (multiview_tryon EXPERIMENTAL pipeline) ────────
# mock | idm_vton | gemini | replicate
VIRTUAL_TRYON_PROVIDER=replicate
# https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_L3YI1JnZpeHjoXV3q9kBnsUpj3zgkzZ3EC0Cz

# ── Stage 2: full-avatar image-to-3D (Unique3D) ─────────────────────────────
# Left at mock (default) — no Unique3D/HuggingFace token configured yet.
# Set to "unique3d" + UNIQUE3D_HF_TOKEN once you have a free HF account
# token (https://huggingface.co/settings/tokens) if you want the final 3D
# mesh to be a real reconstruction instead of a placeholder shell.
# FULL_AVATAR_3D_PROVIDER=unique3d
# UNIQUE3D_HF_TOKEN=hf_xxxxxxxxxxxx
